"""Placeholder module the sub-agents act on.

Unit 4 hands-on: the project is the agent configuration, not the code.
This file exists so the researcher/implementer have something to point at
when you exercise the pipeline. Replace with whatever feature the user
asks for (e.g. an OAuth2 module, an API endpoint, etc.).
"""


def hello() -> str:
    return "hello from code-quality-pipeline"


if __name__ == "__main__":
    print(hello())
